# caddy-dns

Caddy build with dns support.

## Usage

`docker run -it -p 80:80  --rm bebound/caddy-dnspod`

Visit `localhost:80` to see whether caddy is running.

Full dns list: https://github.com/caddy-dns
